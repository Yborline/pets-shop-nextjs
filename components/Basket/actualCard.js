// const actualCard = (clothes, clothesMain) => {

//   const arraysIвBrief = clothes.map((item) => item._id.split("-"));

//   const similarityArray = arraysIвBrief.filter((item) => {
//     return clothesMain.some((item2) => item2._id === item[0]);
//   });

//   // const normalId = array.join("");
//   const arrayId = similarityArray.map((item) => item.join("-"));

//   const newArray = clothes.filter((item) => {
//     return arrayId.some((item2) => item._id === item2);
//   });

//   return newArray;
// };

// export default actualCard;
